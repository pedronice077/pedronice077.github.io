{
  "extends": [
    "development"  // Extending from the development configuration
  ],
  "hints": {
    "axe/name-role-value": [
      "default",
      {
        "link-name": "off"  // Disabling the link-name rule for now
      }
    ],
    "disown-opener": "off"  // Disabling the disown-opener rule as it's not needed
  }
}
